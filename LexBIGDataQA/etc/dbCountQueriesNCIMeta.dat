changeCount|history|select count(*) from ${MYSQL_PREFIX}concepthistory|
changeTally|history|select count(*),editAction,date_format(editDate,'%Y%m') from ${MYSQL_PREFIX}concepthistory group by editAction,editDate|
